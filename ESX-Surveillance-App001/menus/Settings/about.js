(function(){ 
  window.currentModule = { run: function(){ 
    const el = document.querySelector('.report-box'); 
    if (el) el.innerHTML = '<p>ESX Surveillance Tool — Settings page</p>'; 
  }};
})();